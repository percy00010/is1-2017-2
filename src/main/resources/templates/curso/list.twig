<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Listado de Cursos</title>
  </head>
  <body>

 {% if (message) %}
 <div style="color: green; border: 1px solid green; margin-bottom:10px;">{{message}}</div>
 {% endif %}

	<table border="1">
	  <tr>
	    <th>Codigo</th>
	    <th>Creditos</th>
	    <th>Curso</th>
	  </tr>

{% for curso in cursos %}
	  <tr>
	    <td>{{curso.codigo}}</a></td>
	    <td>{{curso.creditos}}</td>
	    <td>{{curso.nombre}}</td>
	  </tr>
{% endfor %}
	</table>
	<br />
	<input type="button" value="Atras" onclick="history.back();" />
  </body>
</html>
